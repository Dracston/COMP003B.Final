@model IEnumerable<COMP003B.Final.Models.Orderproduct>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.productId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.orderId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.orderProductStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.productId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.orderId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.orderProductStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.orderProductId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.orderProductId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.orderProductId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
